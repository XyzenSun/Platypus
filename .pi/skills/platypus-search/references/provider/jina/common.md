# Jina 常用参数

- `--query "关键词"`: 搜索词, 映射为上游 `q`。
- `--num 5`: 结果数量。
- `--type news`: 搜索类型, `web` (网页)、`news` (新闻)、`images` (图片)。
- `--gl US`: 国家或地区, `US` (美国) 或 `CN` (中国)。
- `--hl en`: 搜索语言, `en` (英语) 或 `zh` (中文); 上游实际支持的代码以其 API 为准。
- `--page 2`: 搜索页码。
