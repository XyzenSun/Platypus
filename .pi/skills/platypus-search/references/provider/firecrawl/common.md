# Firecrawl 常用参数

- `--query "关键词"`: 搜索词。
- `--limit 5`: 结果数量。
- `--country US`: 结果地区代码, `US` (美国) 或 `CN` (中国)。
- `--includeDomains '["example.com"]'`: 限定来源域名。
- `--scrapeOptions '{"formats":[{"type":"markdown"}]}'`: 抓取结果页面正文, 增加响应大小与费用。
